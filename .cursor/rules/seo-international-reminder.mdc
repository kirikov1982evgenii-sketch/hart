---
description: Напоминание — SEO для Google EU/US (с 21.05.2026)
alwaysApply: false
---

# SEO: Европа и Америка (Google) — сделать с 21.05.2026

Пользователь попросил **21 мая 2026** (послезавтра от 19.05.2026) выполнить план из чата про индексацию в Google для EU/US.

## Задачи

1. **sitemap.xml** — все публичные страницы + `course.html?id=…` по `data/resources.json`; ссылка в `robots.txt`.
2. **Английские SEO** — `title` / `description` / OG для EN (и при необходимости `hreflang` ru + en на главной и курсах).
3. **Google Search Console** — краткая инструкция для пользователя (подтверждение домена `hart-club.ru`, отправка sitemap).
4. **Проверить HTTPS** на `hart-club.ru` (GitHub Pages Enforce HTTPS); если нет — напомнить открывать `http://` или починить.
5. **Яндекс.Вебмастер** (опционально) — для РФ, если уместно.

## Контекст

- Репозиторий: `kirikov1982evgenii-sketch/hart`, сайт `hart-club.ru`, API `eg1982.pythonanywhere.com`.
- Сейчас в Google сайт **не индексируется**; мета-теги в основном на русском; курсы рендерятся через JS.
- Не коммитить секреты (`.env.local`, токены).

## После выполнения

Удалить или отключить это правило (`alwaysApply: false`).
